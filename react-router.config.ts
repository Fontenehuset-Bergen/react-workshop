import type { Config } from "@react-router/dev/config";

/**
 * Oppsett av react-router, for mer informasjon les
 * https://reactrouter.com/api/framework-conventions/react-router.config.ts
 */
export default {
  appDirectory: "src",
  ssr: true,
} satisfies Config;
