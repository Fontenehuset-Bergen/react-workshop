# Recap av nødvendig javascript kunnskaper
Før vi kan begynne med React er det en stor fordel hvis vi forstår noen viktige konsepter i javascript. 

## immutable vs mutable
React må behandle verdier som immutable for at rendering skal skje, hvis du muterer verdier blir sikke siden alltid re-rendret

## Operatører og kortsluttning
hva er en operatør? hvordan bruker vi det i en if/else og 
hvordan kan vi bruke operatørene && || ?? for bettinget visning av elementer som heter kortsluttning (early escape/short circuiting)

## Funksjoner og ternaries
const func vs function
arrow functions
ternary operations

## template litterals
hvordan kan vi lettere sitte sammen strenger fra variabler og har mer kontroll over formattering?

## Object/array destrucuring og spread
metoder vi kan bruke for å hente ut spesefikke verdier vi er interessert i
kan vi bruke default values?
vi kan hente ut enkle elementer fra en liste med [...myArray] eller fra et objekt med {...myObject}

## Asyncron kode
hva er async kode?
hva er et promise
hva betyr nøkkelordene async og await
kjapt om abortcontroller

## Feil håndtering og fallbacks
hvordan kan vi skrive logikken vår til å håndtere feil og feilmeldinger
hvordan kan vi bruke fallback verdier

## Javascript moduler
commonjs vs es-modules
hvordan importerer og eksporterer vi kode mellom filer

- try catch
- asyncron (promise await etc)
- import (dependencies/libraries) commonjs/ecma
- array prototypes (map, sort, filter, etc)